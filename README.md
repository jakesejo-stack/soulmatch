# SoulMatch Echo First v8 — Stellarium Blend

EchoProfile now uses a live Stellarium Web iframe as a blended background, plus a custom 12 zodiac constellation canvas overlay. The quiz card is visually removed: only centered text, quote, progress, options and buttons remain.

Run:

```powershell
cd "C:\Users\User1\Desktop\SoulMatch3\SoulMatch_Echo_First_v8_StellariumBlend"
npm install
npm run dev
```

Open: http://localhost:5500/echoprofile/
