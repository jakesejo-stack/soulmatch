SoulMatch v30 Working Flow

PUBLIC BEFORE LOGIN:
- /home/ first impression / demo
- /scheme1/, /scheme2/, /scheme3/, /scheme4/ are still accessible as public/demo pages.

AUTH:
- /auth/ is the only Register/Login page.

AFTER LOGIN:
- /member-onboarding/ shows the 5-slide intro.
- Then user spawns in /scheme5/ which is now Echo AI Twin Profile Center.

MEMBER DUPLICATES:
- /member-scheme1/
- /member-scheme2-cities-map/
- /member-scheme3-profiles/
- /member-scheme4-memories/
- /scheme5/ Echo AI Twin center

Start:
npm install
npm run dev
http://localhost:5500/home/
